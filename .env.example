# API base URL (no trailing slash)
VITE_API_URL=http://127.0.0.1:8000

# Refrends master table
# Set to true to enable the new horizontal bulk table (RefrendMasterTable.vue).
# Set to false (or leave unset) to keep the legacy per-becario table.
VITE_REFRENDS_MASTER_TABLE=true
